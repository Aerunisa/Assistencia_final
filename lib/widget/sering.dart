import 'package:flutter/material.dart';
//import 'package:ass/widget/theme.dart';

class SeringMun extends StatelessWidget {
  const SeringMun({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const SizedBox(
          height: 8,
        ),
        Container(),
        //searchingField,
        const SizedBox(
          height: 10,
        ),

        //////////////////// End
        const SizedBox(
          height: 75,
        ),
      ],
    );
  }
}
